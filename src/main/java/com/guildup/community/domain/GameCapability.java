package com.guildup.community.domain;

/** A feature that a configured community game can execute. */
public enum GameCapability {
    ACTIVITY,
    NICKNAME_SYNC,
    TEAM_MAKER,
    KILL_COMPETITION,
    BINGO
}
